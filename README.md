# Heart-Diseaase-Prediction
Heart Disease Prediction web app built with React and Django REST Framework. It uses a trained machine learning model (scikit-learn) to analyze clinical inputs and predict the likelihood of heart disease. Features include real-time form inputs, REST API integration, data scaling, and probability-based predictions.
